# Support (Shared Kernel Base Classes)

Base `Repository`, `Service`, `Action`, and `DTO` classes; standard API Resource
+ pagination envelope; Policy base class; `HasTags`, `HasAudit`, `Taggable` traits.
Every domain module extends these instead of reinventing CRUD scaffolding.
