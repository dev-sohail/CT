# 98. ServerAndHomelabInventory

**Purpose:** Asset registry for your homelab hardware/services

Placeholder module scaffold for the CTLabs Personal Software Ecosystem.
See the master plan in `docs/` for the full 23-field specification.

## Structure
- `Models/` — Eloquent models (module-prefixed tables)
- `Services/` / `Actions/` — business logic
- `Http/Controllers`, `Http/Requests`, `Http/Resources` — REST surface
- `Policies/` — authorization
- `Database/` — migrations + seeders
- `Tests/` — feature + unit tests
