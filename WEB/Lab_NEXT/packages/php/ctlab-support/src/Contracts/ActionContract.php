<?php

namespace Ctlab\Support\Contracts;

/**
 * Marker interface for invokable action classes.
 *
 * Each concrete action defines its own __invoke signature
 * with specific DTO and return types.
 */
interface ActionContract
{
}